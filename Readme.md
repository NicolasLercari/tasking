# Tasking compose

# Run 

Clone repose:
```
   mkdir tasking && cd tasking
   git clone tasking-compose
   git clone tasking-api
   git clone tasking-client 
```

Run: 
```
    cd tasking-compose
    docker-compose up        
```